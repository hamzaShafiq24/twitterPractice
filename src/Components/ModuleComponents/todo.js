import React,{ memo } from "react";
import { StyleSheet, Text, View, SafeAreaView, TouchableOpacity } from 'react-native'

const Todo = () => {

    console.log('todo')
  
    return (
      <View style={{ width: '90%', height: 50, backgroundColor: 'green', borderRadius: 10, alignSelf: 'center' }} >
  
      </View>
    )
  }

//   export default memo(Todo);
  export default Todo;